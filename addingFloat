#include <cs50.h>
#include <stdio.h>

int main(void)
{
    int x = get_int("x: ");
    int  y = get_int("y: ");

    //Divid x by y
    //Casting x and y to a float 
    float z = (float) x / (float) y;

    //Dont forget to change %f
    printf("%f\n", z);
}
